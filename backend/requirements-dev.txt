# Development and test dependencies.
# Runtime deps live in requirements.txt — install both for local work:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
